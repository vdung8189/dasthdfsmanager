package org.esiag.isidis.dast.hdfs.utils;

import static org.junit.Assert.*;


import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class ConfigurationReaderTest {

	@Before
	public void setUp() throws Exception {
	}

	@After
	public void tearDown() throws Exception {
	}

	@Test
	public void testLoadConfig() {
		
		/*assertEquals(null, "localhost", ConfigurationReader.loadconfig().host);
		assertEquals(null, "hdfs", ConfigurationReader.loadconfig().locationName);
		assertEquals(null, "8020", ConfigurationReader.loadconfig().port);*/
		
	}

}
